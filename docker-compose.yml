services:
  mongodb:
    image: mongo:latest
    container_name: finnn-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    command: ["mongod", "--replSet", "rs0", "--bind_ip_all", "--port", "27017"]
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
