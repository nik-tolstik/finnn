export enum DebtType {
  LENT = "lent",
  BORROWED = "borrowed",
}

export enum DebtStatus {
  OPEN = "open",
  CLOSED = "closed",
}

export enum DebtTransactionType {
  CREATED = "created",
  CLOSED = "closed",
  ADDED = "added",
}
